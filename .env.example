# Production Environment Variables
# Copy this to .env in your project root on the server

# Database
MONGO_URI=mongodb+srv://your_username:your_password@cluster.mongodb.net/resellcollege_prod?retryWrites=true&w=majority

# Authentication
JWT_SECRET=your_super_secure_jwt_secret_here_make_it_long_and_random
JWT_EXPIRES_IN=7d

# Domain (without https://)
DOMAIN=yourdomain.com

# API Keys
GEMINI_API_KEY=your_gemini_api_key
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key
VITE_CLOUDINARY_UPLOAD_PRESET=your_cloudinary_preset
VITE_CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name

# Frontend (already configured for production in docker-compose.prod.yml)
# VITE_API_URL is set to /api in the Dockerfile