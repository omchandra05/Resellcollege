# Railway Deployment Configuration

# For Railway, we'll deploy each service separately
# Railway automatically detects Dockerfiles

# Environment Variables to set in Railway dashboard:
# (Copy from .env.example)

# Database URL will be provided by Railway's MongoDB plugin
# Or use MongoDB Atlas free tier

# Frontend build command: npm run build
# Frontend start command: npm run preview (for Vite)

# Backend start command: npm run start